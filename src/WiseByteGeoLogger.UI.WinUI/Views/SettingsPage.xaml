<Page
    x:Class="WiseByteGeoLogger.UI.WinUI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:WiseByteGeoLogger.UI.WinUI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI.UI"
    xmlns:viewmodel="using:WiseByteGeoLogger.UI.WinUI.ViewModels"
    xmlns:xaml="using:Microsoft.UI.Xaml"
    d:DataContext="{d:DesignInstance Type=viewmodel:SettingsViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>
    <ScrollViewer>
        <Grid>
            <StackPanel x:Name="ContentArea">
                <StackPanel>
                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="API Key" />
                    <StackPanel
                        Margin="0,8"
                        Orientation="Horizontal"
                        Spacing="8">
                        <TextBox
                            Width="250"
                            PlaceholderText="API Key"
                            Text="{x:Bind ViewModel.APIKey, Mode=TwoWay}" />
                        <Button
                            VerticalAlignment="Bottom"
                            Command="{x:Bind ViewModel.SaveAPIKeyCommand}"
                            Content="Save" />
                    </StackPanel>
                    <HyperlinkButton
                        Margin="0,0,0,24"
                        Command="{x:Bind ViewModel.GoToLocationIqCommand}"
                        Content="Get your API Key"
                        IsTabStop="False" />
                </StackPanel>

                <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                    <TextBlock x:Uid="Settings_Theme" />

                    <StackPanel Margin="{StaticResource XSmallTopMargin}">
                        <RadioButton
                            x:Uid="Settings_Theme_Light"
                            Command="{x:Bind ViewModel.SwitchThemeCommand}"
                            FontSize="15"
                            GroupName="AppTheme"
                            IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}">
                            <RadioButton.CommandParameter>
                                <xaml:ElementTheme>Light</xaml:ElementTheme>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                        <RadioButton
                            x:Uid="Settings_Theme_Dark"
                            Command="{x:Bind ViewModel.SwitchThemeCommand}"
                            FontSize="15"
                            GroupName="AppTheme"
                            IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}">
                            <RadioButton.CommandParameter>
                                <xaml:ElementTheme>Dark</xaml:ElementTheme>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                        <RadioButton
                            x:Uid="Settings_Theme_Default"
                            Command="{x:Bind ViewModel.SwitchThemeCommand}"
                            FontSize="15"
                            GroupName="AppTheme"
                            IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Default, Mode=OneWay}">
                            <RadioButton.CommandParameter>
                                <xaml:ElementTheme>Default</xaml:ElementTheme>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}" />

                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <TextBlock
                        Margin="0,0,0,16"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}" />

                    <StackPanel>
                        <HyperlinkButton
                            Margin="{StaticResource SettingsPageHyperlinkButtonMargin}"
                            Command="{x:Bind ViewModel.CheckForUpdateCommand}"
                            Content="Check for Update"
                            IsTabStop="False"
                            ToolTipService.ToolTip="Check for Updates" />




                        <Grid ColumnSpacing="4" Visibility="{x:Bind ViewModel.IsWorkingForUpdates, Mode=TwoWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Margin="0,0,0,-4" Text="{x:Bind ViewModel.CurrentUpdateOperation}" />

                            <ProgressBar Grid.Row="1" Value="{x:Bind ViewModel.UpdateProgress}" />

                            <Button
                                Grid.Row="1"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Command="{x:Bind ViewModel.CancelCheckForUpdateCommand}"
                                Content="{ui:SymbolIcon Symbol=Cancel,
                                                        FontSize=14}" />

                            <TextBlock
                                Grid.Row="2"
                                Grid.ColumnSpan="2"
                                Margin="0,-8,0,0"
                                Foreground="{StaticResource TextFillColorSecondary}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind ViewModel.UpdateStatus, Mode=TwoWay}" />

                        </Grid>


                    </StackPanel>

                    <HyperlinkButton
                        Margin="{StaticResource SettingsPageHyperlinkButtonMargin}"
                        Command="{x:Bind ViewModel.GoToSourceCodeCommand}"
                        Content="Source Code" />

                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
